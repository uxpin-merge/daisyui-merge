import * as React from "react";
import Button from "../Button";

export default (
  <Button
    uxpId="button"
    class="btn btn-primary"
    color="primary"
    disabled={false}
    label="Button">
  </Button>
);
